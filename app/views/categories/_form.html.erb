<%= form_for @categories do |f| %>

  <% if @categories.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@categories.errors.count, "error") %> prohibited
        this categories from being saved:
      </h2>
      <ul>
        <% @categories.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.submit %>
  </p>

<% end %>