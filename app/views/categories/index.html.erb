<div class="container-fluid">
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
      <div class="page-header">
        <h1>Categorias</h1>
      </div>
    <div class="col-md-4"></div>
  </div>
</div>

<div class="container-fluid">
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
    	<% @categories.each do |category| %>
            <p><%= link_to category.title, category_path(category), class:'btn btn-default'%></p>
        <% end %>
      <p><%= link_to "Todas as opções", {controller: 'items'}, class:'btn btn-default'%></p>
    </div>
    <div class="col-md-4"></div>
<div>