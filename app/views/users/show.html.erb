<% provide(:title, @user.username) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= @user.username %>
      </h1>
      <p>How are you today?</p>
      <% if admin? %>
      	<p>Você é um admin, parabéns!</p>
        <p>Como admin, tens uma s</p>
      <% else %>
      	<p>Você não é um admin, booo!!</p>
      <% end %>
    </section>
  </aside>
</div>